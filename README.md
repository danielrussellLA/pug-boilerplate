# Development
## install dependencies
`yarn`
## run the template
`v=<template_name> yarn dev`
visit `http://localhost:3000`

# Building Prod Template
`v=<template_name> yarn build`
all files for the s3 website will be in ./dist
